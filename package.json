{
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/index.js",
    "test": "npm run build && node build/"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "tslint": "^6.1.3",
    "typescript": "^4.5.2"
  }
}
