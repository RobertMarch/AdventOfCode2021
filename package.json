{
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "build-start": "npm run build && node build/index.js",
    "build-test": "npm run build && node build/index-test.js",
    "build-watch": "tsc -watch",
    "start": "node build/index.js",
    "test": "node build/index-test.js"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "tslint": "^6.1.3",
    "typescript": "^4.5.2"
  }
}
