{
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "build-start": "npm run build && node build/src/index.js",
    "build-test": "npm run build && node build/src/index-test.js",
    "build-watch": "tsc -watch",
    "start": "node build/src/index.js",
    "test": "node build/src/index-test.js",
    "setup": "node src/setup.js",
    "format": "prettier --write"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "tslint": "^6.1.3",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "prettier": "^2.5.1"
  }
}
